export default function Welcome() {
    // This component is intentionally left empty.
    // The home route ("/") now redirects guests to the login page
    // and authenticated users to the dashboard, so this page
    // should never be shown.
    return null;
}
